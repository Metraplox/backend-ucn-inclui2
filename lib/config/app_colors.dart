import 'package:flutter/material.dart';

class AppColors {
  // Paleta base (ajustar según el branding de la UCN)
  static const Color primary = Color(0xFF003366); // Azul UCN
  static const Color accent = Color(0xFFE3A400); // Dorado UCN

  // Colores para StatisticCard
  static const Color totalStatCard = Color(0xFFE0F7FA); // Celeste claro
  static const Color activeStatCard = Color(0xFFE8F5E9); // Verde claro
  static const Color pendingStatCard = Color(0xFFFFF3E0); // Naranjo claro
  static const Color alertStatCard = Color(0xFFFFEBEE); // Rojo claro
  static const Color neutralStatCard = Color(0xFFF5F5F5); // Gris claro

  // Colores de texto para las tarjetas
  static const Color totalStatText = Color(0xFF006064);
  static const Color activeStatText = Color(0xFF1B5E20);
  static const Color pendingStatText = Color(0xFFE65100);
  static const Color alertStatText = Color(0xFFB71C1C);
  static const Color neutralStatText = Color(0xFF616161);

  // Otros colores de UI
  static const Color background = Color(0xFFF4F6F8);
  static const Color cardBackground = Colors.white;
  static const Color divider = Color(0xFFE0E0E0);
}
